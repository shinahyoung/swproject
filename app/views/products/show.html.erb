<h2 class="text-center">상품 정보 보기</h2>
<div class="row">
    <div class="col-md-1">
        </div>
            <div class="col-md-5 text-right">
<img src="<%= @post.image_url(:medium) %>">
</div>
<div class="col-md-5">

<h3> <%= @post.title %></h3>
    <p><span class="label label-warning"><%= @post.category %></span></p>
      <p><%= @post.content %></p>

<% if @user == 1 %>
        <p><a href="/products/edit/<%= @post.id %>">수정</a> | <a href="/products/delete_complete/<%= @post.id %>">삭제</a></p>
<% end %>
        <p><a href="/">전체 리스트 가기</a></p>
        </div><div class="col-md-1">
        </div>
        </div>
        
</div>
