<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>
 
<h2>안사고 뭐해</h2>
<ul>
<li>품명:가격:수량:포인트:옵션</li>
  <% @cart.line_items.each do |item| %>
      <li><%=item.post.title %>
      :<%=item.price %>
      :<%=item.qty%>
      :<%=item.point%>
      :<%=item.options%></li>
   
        <% end %>
        </ul>


